# تقرير اختبار الإنتاج — 2026-05-23

**الموقع:** https://al-masariha-academy.pages.dev  
**الأداة:** متصفح آلي + فحص يدوي للمسارات  
**بيانات تجريبية:** `REQ-20260521-8416` + `0599112233` (ولي أمر)

---

## ملخص سريع

| المرحلة | النتيجة |
|---------|---------|
| **B — جوال** | ✅ القائمة الجوالية، صفحات التتبع، الانضمام تفتح على عرض ~390px |
| **C — صلاحيات** | ⚠️ لم يُختبر دخول الإدارة (لا كلمة مرور) — صفحة الدخول تعمل |
| **D — مرفقات** | ✅ استكمال ولي أمر يعرض نماذج ومرفقات صحيحة |
| **E — واجهة** | ⚠️ سوشيال «قريباً» ✅ — نص الرئيسية فيه «قاعدة البيانات» (من DB) |
| **F — مسار عام** | ✅ متابعة طلب + استكمال + انضمام |
| **F — إدارة** | ⚠️ يحتاج دخولك (اعتماد/مرفقات) |
| **G — بيانات** | ⏸️ لا يُنفَّذ حذف بدون موافقتك |
| **H — أمان** | ⏸️ مراجعة RLS تحتاج Supabase Dashboard |

---

## E — واجهة + سوشيال

| الاختبار | النتيجة |
|----------|---------|
| تذييل الرئيسية — 5 أيقونات «قريباً» | ✅ |
| صفحة التواصل — سوشيال في العمود الجانبي | ✅ |
| `visitor-message.js` على الإنتاج | ✅ |
| نص البطل في الرئيسية | ⚠️ «من قاعدة البيانات» — **من إعدادات DB** وليس الكود |

**إصلاح مقترح (بعد الرفع التالي):** تحديث وصف البطل من **إعدادات الأكاديمية** أو SQL:

```sql
UPDATE public.academy_settings
SET hero_description = 'انضم للأكاديمية، تابع اللاعبين والمدربين والمباريات، وقدّم طلبك بخطوات واضحة.'
WHERE id = 'default';
```

---

## F — مسار 1 (ولي أمر — بيانات موجودة)

| # | الخطوة | النتيجة |
|---|--------|---------|
| 1 | `request_status` + ref + phone | ✅ يعرض: ولي أمر، جديد، أزرار تواصل وتحضير مرفقات |
| 2 | مراجعة المرفقات في المتابعة | ✅ «لم يتم رفع المرفقات بعد» (متوقع) |
| 3 | `request_completion` + تحقق | ✅ نماذج ولي أمر + 4 مرفقات إجبارية |
| 4 | تقديم طلب لاعب جديد كامل | ⏸️ لم يُرسل (تجنب بيانات إضافية) |
| 5–7 | إدارة → قبول مرفقات → اعتماد | ⏸️ يحتاج دخول المدير |

---

## F — مسارات أخرى (واجهة عامة)

| المسار | النتيجة |
|--------|---------|
| صفحة الانضمام — 5 أنواع | ✅ لاعب، ولي أمر، عضوية، كادر، داعم |
| المتجر | ✅ الصفحة + نموذج طلب؛ شبكة منتجات تحتاج تحقق بصري (قد تكون فارغة أو بطيئة التحميل) |
| `store_order_status` | ⏸️ لم يُختبر برقم ORD |
| التواصل — نموذج | ⏸️ لم يُرسل (تجنب رسائل تجريبية) |
| `admin_requests` بدون دخول | ✅ إعادة توجيه إلى `admin_login` |

---

## B — جوال (محاكاة 390×844)

| الصفحة | النتيجة |
|--------|---------|
| المتجر | ✅ زر «فتح القائمة» |
| التواصل | ✅ نفس السلوك |
| الانضمام | ✅ النموذج ظاهر |

---

## G — بيانات (لم يُنفَّذ)

- لم يُحذف شيء.
- **توصية:** نسخة احتياطي Supabase قبل أي تنظيف → `docs/PHASE_G_DATA_OPERATIONS.md`.

---

## H — أمان (لم يُنفَّذ من اللوحة)

- مراجعة `pg_policies` تحتاج تنفيذ SQL في Supabase → `docs/PHASE_H_SECURITY_LAUNCH_CHECKLIST.md`.
- 2FA: توصية للمالك فقط.

---

## ما يحتاجك أنت (5–15 دقيقة)

1. **دخول الإدارة** → افتح `REQ-20260521-8416` → تأكد من **مراجعة المرفقات** في المودال.
2. **تحديث وصف الرئيسية** (SQL أعلاه أو من لوحة الإعدادات).
3. **مسار لاعب كامل** من الانضمام حتى الاعتماد (إن أردت تأكيد D بالكامل).

---

## الحكم

**جاهز للتشغيل التجريبي على pages.dev** مع ملاحظتين:
1. إصلاح نص «قاعدة البيانات» في الرئيسية (إعدادات).
2. اختبار الإدارة (اعتماد + مرفقات) بحسابك.
